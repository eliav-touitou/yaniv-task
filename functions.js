cards = [];
function createCards(){
         for(let i = 0, i < suits.length, i++ ){
        for(let j = 0, j < rank.length, j++){
            cards.push(new Card(suit[i],rank[j] ))
        }
        cards.push(new Card(Joker,null, true ))
        cards.push(new Card(Joker,null, true ))
         }
    

function handScore() {
    let score = 0;
    for (let card )

        }
    
    
